export type UserTableType = {
  name: string,
  role: string,
  userId: string,
  userName: string,
  status: string,
  sumWork: number,
  teacherId: string,
  procent: number,
  createdAt: number,
  ban: boolean,
}